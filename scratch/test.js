const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE ERROR:', err.message)); try { await page.goto('http://localhost:1420', {waitUntil:'networkidle0', timeout:10000}); await new Promise(r=>setTimeout(r,1000)); console.log('Clicking journey summary...'); await page.click('.journey-summary'); await new Promise(r=>setTimeout(r,500)); const details = await page..journey-details; console.log('Details found?', !!details); } catch(e) { console.log('ERR:', e.message); } await browser.close(); })();
